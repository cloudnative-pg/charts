mode: recovery

cluster:
  instances: 2

recovery:
  existingSecret:
    name: source-cluster-replication
  method: pgBasebackup
  methodSettings:
    pgBasebackup:
      connectionParameters:
        host: source-cluster-rw
        database: mygooddb
        username: streaming_replica
        sslMode: require
      auth: tls
