##
# This is a test that verifies that recovery target options are correctly propagated to the CNPG cluster.
# P.S. This test is not designed to have a good running configuration, it is designed to test the configuration propagation!
---
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: recovery-target-exclusive
spec:
  timeouts:
    apply: 1s
    assert: 5s
    cleanup: 30s
  steps:
    - name: Install backup recovery cluster target exclusive
      try:
        - script:
            content: |
              helm upgrade \
                --install \
                --namespace $NAMESPACE \
                --values ./06-recovery_target_exclusive.yaml \
                --wait \
                recovery-target-exclusive ../../
        - assert:
            file: ./06-recovery_target_exclusive-assert.yaml
    - name: Cleanup
      try:
        - script:
            content: |
              helm uninstall --namespace $NAMESPACE recovery-target-exclusive
