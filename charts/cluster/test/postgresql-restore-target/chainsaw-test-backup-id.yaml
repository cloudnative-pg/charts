##
# This is a test that verifies that restore target options are correctly propagated to the CNPG cluster.
# P.S. This test is not designed to have a good running configuration, it is designed to test the configuration propagation!
---
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: recovery-target-backup-id
spec:
  timeouts:
    apply: 1s
    assert: 5s
    cleanup: 30s
  steps:
    - name: Install backup recovery cluster target backup id
      try:
        - script:
            content: |
              helm upgrade \
                --install \
                --namespace $NAMESPACE \
                --values ./01-recovery_target_backup_id.yaml \
                --wait \
                recovery-target-backup-id ../../
        - assert:
            file: ./01-recovery_target_backup_id-assert.yaml
    - name: Cleanup
      try:
        - script:
            content: |
              helm uninstall --namespace $NAMESPACE recovery-target-backup-id
