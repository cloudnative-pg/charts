The {{ include "cluster.fullname" . }} has been installed successfully.

Cheatsheet
----------

Run Helm Tests:
helm test --namespace {{ .Release.Namespace }} {{ .Release.Name }}

Get a list of all base backups:
kubectl --namespace {{ .Release.Namespace }} get backups --selector cnpg.io/cluster={{ include "cluster.fullname" . }}

Connect to the cluster's primary instance:
kubectl --namespace {{ .Release.Namespace }} exec --stdin --tty services/{{ include "cluster.fullname" . }}-rw -- bash